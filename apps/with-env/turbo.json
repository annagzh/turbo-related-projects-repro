{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "!vercel.json"],
      "outputs": ["dist/**"],
      "env": ["VERCEL_RELATED_PROJECTS"]
    }
  }
}
